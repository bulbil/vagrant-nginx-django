[uwsgi]
project = project
base = /srv/apps

chdir = %(base)/%(project)
home = %(base)/Env/%(project)
module = %(project).wsgi:application

master = true
processes = 5

uid = www-data
gid = www-data
socket = %(base)/%(project)/%(project).sock
chmod-socket = 664
vacuum = true
